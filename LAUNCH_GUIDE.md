# 康洁工程部小程序 — 正式上线步骤（简单版）

不用懂编程，按下面顺序一步步做就行。

**「云开发」在哪里？**  
在微信开发者工具窗口的**最上面一栏**（和「编译」「预览」「上传」同一排），有一个 **「云开发」** 按钮，点一下就会打开云开发面板。后面说的「点云开发」都是点这里。

---

## 第一步：填好「云开发环境」

1. 用**微信开发者工具**打开这个项目。
2. 点最上面一栏的 **「云开发」**。
   - 如果还没开通过：先按提示开通，并创建一个环境，会给你一串**环境 ID**（像 `xxxx-1gxxxxx`）。
   - 如果已经开通过：在云开发页面里能看到**环境 ID**，复制下来。
3. 在开发者工具左边，找到 **「miniprogram」** 点开，再点开里面的 **「app.js」**。
4. 在打开的文件里找到 **`YOUR_CLOUD_ENV_ID`** 这几个字，把它**整段**删掉，**粘贴**你刚才复制的环境 ID（保留前后引号，不要多删）。
5. 按 **Ctrl+S**（或 Cmd+S）保存。

---

## 第二步：把「云函数」都传到云端

1. 在开发者工具左边，找到 **「cloudfunctions」** 这个文件夹，点开。
2. 里面有很多小文件夹（getMe、getAssetByQr、submitReplacementLog 等），**每一个**都这样操作：
   - 在文件夹上**点右键**；
   - 选 **「上传并部署：云端安装依赖」**；
   - 等它提示成功。
3. 全部传完后，再点最上面一栏的 **「云开发」**，在打开的云开发面板里点 **「云函数」**，看列表里是不是都有这些名字，有就对了。

---

## 第三步：准备「数据库」

1. 点最上面一栏的 **「云开发」**，在打开的云开发面板里点 **「数据库」**。
2. 如果里面还是空的，需要**新建集合**（点「添加集合」）：
   - 建议先建这些名字（一个名字一个集合）：  
     **assets**、**parts**、**replacement_logs**、**alerts**、**users**。  
   - 如果提示还要别的名字，就按提示再建。
3. 建好后，每个集合点进去是空的也没关系，后面可以导入或录入数据。

---

## 第四步：删掉测试数据（如果之前用过「初始化测试数据」）

1. 还是点最上面一栏 **「云开发」**，再点 **「数据库」**。
2. 逐个点开每个集合（如 replacement_logs、alerts、assets 等）。
3. 如果里面有数据，且你确认都是**测试用的**：
   - 勾选**全部记录**；
   - 点 **「删除」**。
4. 如果希望**只删业务数据、保留设备和用户**：  
   只清空 **replacement_logs**、**monthly_part_usage**、**alerts** 这三个；  
   **assets**、**parts**、**users** 可以保留或改成真实数据。

---

## 第五步：导入真实数据（设备、配件、用户等）

- 如果公司有现成的设备清单、配件表，可以按小程序里「数据管理」页的**导入模板**整理成 Excel，在手机小程序上用「数据管理 → 导入」上传。
- 如果没有模板，可以找技术人员用云开发控制台或云函数帮你导入第一批设备、配件、用户。

---

## 第六步：用手机试一遍

1. 在微信开发者工具右上角点 **「预览」**，用手机微信扫出现的二维码。
2. 在手机上试：能打开、能扫码进设备、能提交一条更换记录、能在「记录」和「看板」里看到数据。  
   有问题就记下来，找技术人员对照检查。

---

## 第七步：上传并提交微信审核

1. 在微信开发者工具右上角点 **「上传」**。
2. 版本号填：**1.0.0**，备注可以填：**康洁工程部正式版**，然后确定上传。
3. 打开浏览器，登录 **微信公众平台**（mp.weixin.qq.com），进你的小程序 → **版本管理**。
4. 在「开发版本」里能看到刚传的 1.0.0，点 **「提交审核」**，按页面提示选类目、填说明，提交。
5. 等审核通过后，在「版本管理」里点 **「发布」**，小程序就正式上线了。

---

## 你可以这样打勾

- [ ] 第一步：填好云开发环境 ID 并保存
- [ ] 第二步：所有云函数都「上传并部署」成功
- [ ] 第三步：数据库里建好需要的集合
- [ ] 第四步：删掉测试数据（如果有）
- [ ] 第五步：导入或录入真实设备、配件、用户
- [ ] 第六步：手机预览试一遍没问题
- [ ] 第七步：上传 → 提交审核 → 发布

做完这七步，上线就完成了。有某一步不清楚的，可以把第几步、卡在哪里告诉技术人员，他们能对着帮你做。
