# 云南康洁 v1.0 — 测试执行报告（合并版）

> 执行日期：2026-02-13
> 范围：小程序 + PC管理端 + 跨端一致性 + 安全权限
> 本文件由三份独立报告合并而成

---

# 第一部分：小程序功能测试

> 范围：第二部分 — 小程序全部功能

---

# 🔴 代码审查发现的严重问题（测试前必读）

在逐行审查代码后，发现以下 **缺失的云函数** 会导致多个页面直接报错：

## 问题 A：缺失云函数清单

`api.js` 中调用了以下云函数，但 `cloudfunctions/` 目录下 **没有对应实现**：

| 缺失的云函数 | 被哪个页面调用 | 影响范围 | 严重程度 |
|:------------|:-------------|:--------|:-------:|
| `getDashboard` | 看板页 `dashboard/index.js` | 看板页整个无数据 | 🔴 高 |
| `getPartUsageDetail` | 配件下钻页 `dashboard/part-detail` | 配件消耗明细无法打开 | 🔴 高 |
| `getAssetUsageDetail` | 设备下钻页 `dashboard/asset-detail` | 设备更换明细无法打开 | 🔴 高 |
| `getAlertDetail` | 报警详情页 `alerts/detail.js` | 报警详情页无数据 | 🔴 高 |
| `getAssetAlerts` | 设备报警分布页 `dashboard/asset-alerts` | 设备报警下钻无法打开 | 🟡 中 |
| `listAssets` | 更换记录页 `record/index.js` | 记录页「按设备筛选」下拉为空 | 🟡 中 |
| `adminDashboard` | 看板成本排名、AI 报告等 | 配件金额排名、AI 报告不可用 | 🔴 高 |

> **结论**：看板页、报警详情页、所有下钻页面在真实环境（USE_MOCK=false）下**会直接报错白屏**。

## 问题 B：已有 17 个云函数 vs 需要的云函数

**已部署的**（17个）：
`getMe`、`getAssetByQr`、`getLocationsAndParts`、`submitReplacementLog`、`listReplacementLogs`、`listAlerts`、`ackAlert`、`bindUser`、`getFactories`、`exportData`、`importData`、`initMockData`、`adminPcLogin`、`pcGateway`、`adminFactoryManage`、`adminInventoryManage`、`adminInventoryInbound`

**缺失的**（7个）：
`getDashboard`、`getPartUsageDetail`、`getAssetUsageDetail`、`getAlertDetail`、`getAssetAlerts`、`listAssets`、`adminDashboard`

---

# 第二部分：小程序测试执行

---

## 2.1 账号绑定

### 测试用例 2.1.1：首次打开 — 未绑定用户自动跳转

| 项目 | 内容 |
|------|------|
| **前置条件** | 使用一个 users 集合中 **没有 openid** 的微信号打开小程序 |
| **操作步骤** | 打开小程序 |
| **预期结果** | `app.js` 的 `initLogin()` 调用 `getMe` → 返回 `AUTH_NOT_BOUND` → 跳转到 `/pages/bind/index` |
| **代码依据** | `app.js` 第 46-49 行：`if (result.error.code === 'AUTH_NOT_BOUND')` → `wx.navigateTo({ url: '/pages/bind/index' })` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.1.2：正确绑定

| 项目 | 内容 |
|------|------|
| **前置条件** | 在绑定页面，数据库中有对应 username 且 status=active 的用户 |
| **操作步骤** | 输入正确的用户名和密码 → 点击「绑定账号」 |
| **预期结果** | 1. 显示「绑定中...」loading<br>2. 云函数 `bindUser` 验证密码（支持明文和 SHA256 两种）<br>3. 绑定 openid 到该用户记录<br>4. 提示「绑定成功！」<br>5. 1 秒后跳转到扫码页 |
| **代码依据** | `bind/index.js` 第 34-51 行；`bindUser` 云函数第 47-50 行密码验证逻辑 |
| **注意事项** | ⚠️ 如果该 openid 已绑定其他账号，会返回 `ALREADY_BOUND` 错误（`bindUser` 第 21-29 行）<br>⚠️ 如果该 username 已被其他微信绑定，会返回 `BOUND_TO_OTHER` 错误（`bindUser` 第 57-62 行） |
| **测试结果** | |
| **备注** | |

### 测试用例 2.1.3：错误密码

| 项目 | 内容 |
|------|------|
| **操作步骤** | 输入正确用户名 + 错误密码 → 点击绑定 |
| **预期结果** | 页面显示红字错误信息「密码错误」 |
| **代码依据** | `bindUser` 云函数第 53 行返回 `AUTH_FAILED`；`bind/index.js` 第 53-56 行显示 errorMsg |
| **测试结果** | |
| **备注** | |

### 测试用例 2.1.4：空输入校验

| 项目 | 内容 |
|------|------|
| **操作步骤** | 用户名为空 → 点击绑定 |
| **预期结果** | 页面显示「请输入用户名」（前端拦截，不发请求） |
| **代码依据** | `bind/index.js` 第 22-25 行：`if (!username.trim())` → `errorMsg: '请输入用户名'` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.1.5：重新打开保持登录

| 项目 | 内容 |
|------|------|
| **操作步骤** | 绑定成功后，关闭小程序再打开 |
| **预期结果** | 直接进入首页，不再显示绑定页 |
| **代码依据** | `auth.js` 第 8-13 行从 `wx.getStorageSync('kj_user')` 恢复缓存；`app.js` 第 37-44 行 `getMe` 返回 ok 则直接设置用户 |
| **测试结果** | |
| **备注** | |

---

## 2.2 扫码录入（核心流程）

### 测试用例 2.2.1：扫码页面 UI

| 项目 | 内容 |
|------|------|
| **操作步骤** | 进入「扫码录入」Tab |
| **预期结果** | 1. 扫码按钮垂直居中（flex + min-height:50vh）<br>2. 显示「扫码录入」文字和提示<br>3. 开发调试入口**不显示**（IS_DEV=false） |
| **代码依据** | `scan/index.wxml` 第 16 行 `wx:if="{{devMode}}"` + `index.js` 第 5 行 `IS_DEV = false` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.2：扫描有效二维码

| 项目 | 内容 |
|------|------|
| **前置条件** | assets 集合中有 assetId 为二维码内容的设备记录 |
| **操作步骤** | 点击扫码按钮 → 扫描设备二维码 |
| **预期结果** | 1. 调起相机扫码<br>2. 显示「查询设备...」loading<br>3. 跳转到设备详情页 `/pages/asset/detail?assetId=xxx`<br>4. 扫码页显示「最近扫描的设备」卡片 |
| **代码依据** | `scan/index.js` 第 26-47 行完整流程 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.3：扫描无效二维码

| 项目 | 内容 |
|------|------|
| **操作步骤** | 扫描一个随机二维码（assetId 不存在） |
| **预期结果** | Toast 提示「未找到该设备」 |
| **代码依据** | `scan/index.js` 第 34-36 行：`result.ok` 为 false 时 showToast |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.4：取消扫码

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击扫码后点「取消」 |
| **预期结果** | 静默返回，不弹任何提示 |
| **代码依据** | `scan/index.js` 第 51 行：`e.errMsg.indexOf('cancel') > -1` 时 return |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.5：更换表单 — 完整提交

| 项目 | 内容 |
|------|------|
| **前置条件** | 1. 使用 **Engineer** 角色账号<br>2. 设备已配置部位和配件映射 |
| **操作步骤** | 设备详情页 → 更换登记 → 选更换类型 → 选部位 → 选配件（多选）→ 设数量 → 拍照上传 → 提交 |
| **预期结果** | 1. 显示当前操作人和日期<br>2. 部位下拉显示该设备所有部位<br>3. 选部位后配件列表更新为该部位配件<br>4. 可多选配件并设置数量<br>5. 可拍照或选照片<br>6. 提交成功显示「提交成功（本月累计已更新）」<br>7. 1.5 秒后自动返回 |
| **代码依据** | `replace/form.js` 全文，特别是 `onSubmit()` 第 162-207 行 |
| **⚠️ 重要** | 只有 **Engineer** 角色能提交！Supervisor/Admin 提交会被云函数拒绝（`submitReplacementLog` 第 22-24 行：`if (user.role !== 'Engineer')` → PERMISSION_DENIED） |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.6：更换类型验证

| 项目 | 内容 |
|------|------|
| **操作步骤** | 查看更换类型下拉选项 |
| **预期结果** | 只有三种类型：「维修」「预防」「紧急」 |
| **代码依据** | `submitReplacementLog` 第 36 行：`if (!['维修', '预防', '紧急'].includes(type))` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.7：必填校验 — 未选配件

| 项目 | 内容 |
|------|------|
| **操作步骤** | 选了类型、部位，但不选配件，直接提交 |
| **预期结果** | 提交按钮灰色不可点击（canSubmit=false） |
| **代码依据** | `replace/form.js` 第 140-147 行 `checkCanSubmit()`：需要 hasType && hasLocation && hasSkus && hasImages 全部为 true |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.8：必填校验 — 未上传照片

| 项目 | 内容 |
|------|------|
| **操作步骤** | 选了类型、部位、配件，但不上传照片 |
| **预期结果** | 提交按钮灰色不可点击（images.length >= 1 未满足） |
| **代码依据** | `replace/form.js` 第 145 行 `const hasImages = images.length >= 1` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.9：幂等性 — 重复提交

| 项目 | 内容 |
|------|------|
| **操作步骤** | 短时间内连续点击提交 |
| **预期结果** | 1. 提交按钮在提交中变为 disabled<br>2. 即使两次请求到达服务器，第二次因 clientOfflineId 相同，返回 `duplicate: true`，不重复写入 |
| **代码依据** | `replace/form.js` 第 169 行 `submitting: true` 禁用按钮；`submitReplacementLog` 第 56-62 行幂等检查 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.10：放弃填写确认

| 项目 | 内容 |
|------|------|
| **操作步骤** | 已选择配件后，点击返回/放弃 |
| **预期结果** | 弹出确认弹窗「当前已填写内容将丢失，确定放弃吗？」 |
| **代码依据** | `replace/form.js` 第 124-138 行 `onAbandonTap()` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.2.11：部位无配件提示

| 项目 | 内容 |
|------|------|
| **前置条件** | 某部位在 location_part_map 中无配件映射 |
| **操作步骤** | 在表单中选择该部位 |
| **预期结果** | 弹窗提示「该部位尚未配置可用配件」 |
| **代码依据** | `replace/form.js` 第 84-89 行：`skus.length === 0` 时 showModal |
| **测试结果** | |
| **备注** | |

---

## 2.3 更换记录页

### 测试用例 2.3.1：记录列表展示

| 项目 | 内容 |
|------|------|
| **操作步骤** | 进入「记录」Tab |
| **预期结果** | 显示当月更换记录列表，每条记录显示设备名、配件数量、操作人、时间 |
| **代码依据** | `record/index.js` 第 23-27 行 onLoad → 加载当月数据 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.3.2：按设备筛选

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击设备筛选下拉 → 选择某个设备 |
| **预期结果** | ⚠️ **可能失败** — 设备下拉需要调用 `listAssets` 云函数，该云函数**不存在**！ |
| **代码依据** | `record/index.js` 第 30-41 行 `loadAssets()` → `api.listAssets()` → `callCloud('listAssets', {})` |
| **预计问题** | 🔴 设备筛选下拉为空或报错（listAssets 云函数缺失） |
| **测试结果** | |
| **备注** | |

### 测试用例 2.3.3：按月筛选

| 项目 | 内容 |
|------|------|
| **操作步骤** | 切换月份选择器到上个月 |
| **预期结果** | 列表刷新为上个月的记录（有防抖 300ms） |
| **代码依据** | `record/index.js` 第 115-118 行 `onMonthChange` → `_debouncedLoadList()` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.3.4：下拉刷新 + 上拉加载

| 项目 | 内容 |
|------|------|
| **操作步骤** | 1. 下拉页面触发刷新<br>2. 滚动到底部触发加载 |
| **预期结果** | 1. 下拉：列表重新加载<br>2. 上拉：追加下一页（每页 20 条） |
| **代码依据** | `record/index.js` 第 69-77 行 `onPullDownRefresh` / `onReachBottom` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.3.5：记录详情弹窗

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击某条记录 |
| **预期结果** | 弹窗显示详情：设备名、部位、配件清单(名称×数量)、备注 |
| **代码依据** | `record/index.js` 第 156-165 行 `onItemTap` → showModal |
| **测试结果** | |
| **备注** | |

---

## 2.4 报警页面

### 测试用例 2.4.1：报警列表

| 项目 | 内容 |
|------|------|
| **操作步骤** | 进入「阈值报警」Tab |
| **预期结果** | 1. 显示两个报警类型切换：阈值报警 / 库存报警<br>2. 显示未处理/已处理 Tab<br>3. 列表显示当月报警 |
| **代码依据** | `alerts/index.js` 第 87-116 行 `loadList` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.4.2：触发阈值报警

| 项目 | 内容 |
|------|------|
| **前置条件** | 1. asset_part_thresholds 中某设备某配件的月阈值设为 **1**<br>2. 该配件本月累计用量为 0 |
| **操作步骤** | 用 Engineer 账号扫该设备 → 更换登记 → 选该配件数量 2 → 提交 |
| **预期结果** | 提交成功后，alerts 集合新增一条 OPEN 状态的报警记录 |
| **代码依据** | `submitReplacementLog` 第 155-194 行：`newQtySum > thresholdMonthly` 时创建 alert |
| **验证方式** | 提交后切换到「阈值报警」Tab，应能看到新报警 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.4.3：报警详情页

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击报警列表中的某条报警 |
| **预期结果** | ⚠️ **可能失败** — 报警详情页调用 `getAlertDetail` 云函数，该云函数**不存在**！ |
| **代码依据** | `alerts/detail.js` 第 19-30 行 `loadAlert()` → `api.getAlertDetail(this.alertId)` → `callCloud('getAlertDetail', { alertId })` |
| **预计问题** | 🔴 报警详情页白屏或报错（getAlertDetail 云函数缺失） |
| **测试结果** | |
| **备注** | |

### 测试用例 2.4.4：已知晓确认（如详情页正常）

| 项目 | 内容 |
|------|------|
| **前置条件** | 使用 Supervisor 或 Admin 账号 |
| **操作步骤** | 进入报警详情 → 输入确认说明 → 点击「已知晓」 |
| **预期结果** | 确认成功，报警状态变为 ACK |
| **代码依据** | `alerts/detail.js` 第 37-59 行 `onAck()`；`auth.js` 第 97-99 行 `canAck()` 检查角色 |
| **注意** | 工程师角色 canAck=false，不显示「已知晓」按钮 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.4.5：已知晓必填确认说明

| 项目 | 内容 |
|------|------|
| **操作步骤** | 不输入确认说明直接点击「已知晓」 |
| **预期结果** | Toast 提示「请填写确认说明」 |
| **代码依据** | `alerts/detail.js` 第 38-40 行：`if (!this.data.ackNote)` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.4.6：库存报警查看

| 项目 | 内容 |
|------|------|
| **操作步骤** | 切换到「库存报警」Tab |
| **预期结果** | 显示低库存报警列表；点击条目弹窗显示详情 |
| **代码依据** | `alerts/index.js` 第 119-135 行 `loadInventoryAlerts`；第 144-151 行 `onInventoryAlertTap` 弹窗 |
| **测试结果** | |
| **备注** | |

---

## 2.5 数据看板

### 测试用例 2.5.1：看板页面加载

| 项目 | 内容 |
|------|------|
| **操作步骤** | 进入「看板」Tab |
| **预期结果** | ⚠️ **可能失败** — 看板调用 `getDashboard` 云函数，该云函数**不存在**！ |
| **代码依据** | `dashboard/index.js` 第 46-73 行 `loadDashboard()` → `api.getDashboard(...)` → `callCloud('getDashboard', params)` |
| **预计问题** | 🔴 看板页面无数据或报错（getDashboard 云函数缺失） |
| **测试结果** | |
| **备注** | |

### 测试用例 2.5.2：配件金额排名

| 项目 | 内容 |
|------|------|
| **前置条件** | canViewCost=true（Supervisor/Management/Admin） |
| **操作步骤** | 看板页面加载后查看「配件使用金额」区域 |
| **预期结果** | ⚠️ **可能失败** — 调用 `adminDashboard` 云函数，该云函数**不存在**！ |
| **代码依据** | `dashboard/index.js` 第 76-97 行 `loadCostRanking()` → `api.getMonthlyCostRanking(...)` → `callCloud('adminDashboard', ...)` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.5.3：月份切换

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击左箭头切换到上月 → 点击右箭头切换回当月 |
| **预期结果** | 1. 月份标签更新<br>2. 当月时右箭头不可点击<br>3. 切换后重新加载数据 |
| **代码依据** | `dashboard/index.js` 第 106-132 行 `onPrevMonth/onNextMonth` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.5.4：排行下钻

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击配件排行中某个配件 |
| **预期结果** | ⚠️ 跳转到 `/pages/dashboard/part-detail`，但该页面调用 `getPartUsageDetail` **云函数不存在** |
| **预计问题** | 🔴 下钻页面无数据或报错 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.5.5：看板 → 报警联动

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击看板中的「未处理报警」数字 |
| **预期结果** | 跳转到报警 Tab 并自动筛选为 OPEN 状态 |
| **代码依据** | `dashboard/index.js` 第 135-140 行 `onOpenAlertsTap` → `alertFilterStatus = 'OPEN'` → `switchTab` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.5.6：看板 → 趋势图 → 记录联动

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击趋势图中某天的柱子 |
| **预期结果** | 跳转到记录 Tab，并自动筛选该日期的记录 |
| **代码依据** | `dashboard/index.js` 第 214-222 行 `onTrendDayTap` → `recordFilterDate` → 记录页 onShow 接收 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.5.7：看板 → 人员 → 记录联动

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击人员工作量排行中某个工程师 |
| **预期结果** | 跳转到记录 Tab，筛选该工程师的记录 |
| **代码依据** | `dashboard/index.js` 第 202-212 行 `onEngineerTap` → `recordFilterUser` |
| **测试结果** | |
| **备注** | |

---

## 2.6 我的页面

### 测试用例 2.6.1：用户信息显示

| 项目 | 内容 |
|------|------|
| **操作步骤** | 进入「我的」Tab |
| **预期结果** | 1. 显示用户名和角色标签<br>2. 显示当前工厂名称<br>3. 显示版本号 v1.0.0 |
| **代码依据** | `me/index.wxml` 第 4-9 行用户卡片；`me/index.js` 第 18-37 行 onShow |
| **测试结果** | |
| **备注** | |

### 测试用例 2.6.2：工厂切换（Management/Admin）

| 项目 | 内容 |
|------|------|
| **前置条件** | 使用 Management 或 Admin 角色账号，数据库中有多个工厂 |
| **操作步骤** | 点击「当前工厂」行的「切换 ›」 |
| **预期结果** | 弹出 ActionSheet 显示工厂列表，选择后切换成功 |
| **代码依据** | `me/index.js` 第 59-72 行 `onSwitchFactory`；`auth.js` 第 118-119 行 `canSwitchFactory` 仅 Management/Admin |
| **测试结果** | |
| **备注** | |

### 测试用例 2.6.3：工厂切换（Engineer/Viewer）

| 项目 | 内容 |
|------|------|
| **前置条件** | 使用 Engineer 或 Viewer 角色账号 |
| **操作步骤** | 查看「当前工厂」行 |
| **预期结果** | **不显示** 「切换 ›」箭头，不可切换 |
| **代码依据** | `me/index.wxml` 第 20 行 `wx:if="{{canSwitchFactory}}"` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.6.4：AI 分析报告入口（主管及以上）

| 项目 | 内容 |
|------|------|
| **前置条件** | 使用 Supervisor/Management/Admin 角色 |
| **操作步骤** | 查看「我的」页面 |
| **预期结果** | 显示「AI 分析报告」入口卡片 |
| **代码依据** | `me/index.wxml` 第 28 行 `wx:if="{{canManage}}"` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.6.5：AI 分析报告入口（工程师/查看员）

| 项目 | 内容 |
|------|------|
| **前置条件** | 使用 Engineer 或 Viewer 角色 |
| **预期结果** | **不显示** AI 分析报告入口 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.6.6：切换账号/退出

| 项目 | 内容 |
|------|------|
| **操作步骤** | 点击「切换账号」按钮 |
| **预期结果** | 1. 弹窗确认「退出当前账号后需要重新绑定微信。确定要退出吗？」<br>2. 确认后清除本地缓存<br>3. 跳转到绑定页面 |
| **代码依据** | `me/index.js` 第 79-95 行 `onLogout`：`auth.logout()` → `wx.removeStorageSync('kj_user')` → navigateTo bind |
| **测试结果** | |
| **备注** | |

---

## 2.7 离线功能

### 测试用例 2.7.1：离线提交

| 项目 | 内容 |
|------|------|
| **操作步骤** | 1. 关闭手机网络<br>2. 进入更换表单填写并提交 |
| **预期结果** | 1. 提交时走入 catch 分支<br>2. 提示「提交失败，已保存待同步」<br>3. 离线队列 +1（offlineCount 增加） |
| **代码依据** | `replace/form.js` 第 188 行 `offlineQueue.enqueue`（提交前入队）；第 201-206 行 catch 提示 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.7.2：离线横幅显示

| 项目 | 内容 |
|------|------|
| **操作步骤** | 有离线记录时查看扫码页和我的页面 |
| **预期结果** | 页面顶部显示离线提示横幅，显示待同步数量 |
| **代码依据** | `scan/index.wxml` 第 4 行 `<offline-banner />`；`me/index.wxml` 第 25 行 |
| **测试结果** | |
| **备注** | |

### 测试用例 2.7.3：手动同步

| 项目 | 内容 |
|------|------|
| **操作步骤** | 恢复网络 → 点击离线横幅的「同步」按钮 |
| **预期结果** | 1. 显示同步中状态<br>2. 逐条调用 submitReplacementLog<br>3. 成功后提示「同步完成」<br>4. offlineCount 变为 0 |
| **代码依据** | `offline-queue.js` 第 65-91 行 `syncAll()`；`scan/index.js` 第 80-93 行 `onSync()` |
| **测试结果** | |
| **备注** | |

### 测试用例 2.7.4：幂等保护

| 项目 | 内容 |
|------|------|
| **操作步骤** | 离线记录同步后，再次手动同步 |
| **预期结果** | 队列为空，不发送请求 |
| **代码依据** | `offline-queue.js` 第 69 行：`if (queue.length === 0) return` |
| **测试结果** | |
| **备注** | |

---

# 第二部分测试总结

## 发现的问题汇总

| 编号 | 严重程度 | 问题 | 影响页面 | 状态 |
|:----:|:-------:|------|---------|:----:|
| BUG-01 | 🔴 高 | `getDashboard` 云函数不存在 | 看板页面无数据 | 待修复 |
| BUG-02 | 🔴 高 | `getAlertDetail` 云函数不存在 | 报警详情页无数据 | 待修复 |
| BUG-03 | 🔴 高 | `getPartUsageDetail` 云函数不存在 | 配件下钻页面无数据 | 待修复 |
| BUG-04 | 🔴 高 | `getAssetUsageDetail` 云函数不存在 | 设备下钻页面无数据 | 待修复 |
| BUG-05 | 🟡 中 | `getAssetAlerts` 云函数不存在 | 设备报警分布下钻无数据 | 待修复 |
| BUG-06 | 🟡 中 | `listAssets` 云函数不存在 | 记录页设备筛选下拉为空 | 待修复 |
| BUG-07 | 🔴 高 | `adminDashboard` 云函数不存在 | 成本排名和 AI 报告不可用 | 待修复 |

## 可正常工作的功能 ✅

基于代码审查，以下功能链路的云函数**全部存在**，应能正常工作：

| 功能 | 涉及的云函数 | 状态 |
|------|:----------:|:----:|
| 账号绑定（bind） | `getMe` + `bindUser` | ✅ 正常 |
| 扫码查设备 | `getAssetByQr` | ✅ 正常 |
| 获取部位配件 | `getLocationsAndParts` | ✅ 正常 |
| 提交更换记录 | `submitReplacementLog` | ✅ 正常 |
| 更换记录列表 | `listReplacementLogs` | ✅ 正常 |
| 报警列表 | `listAlerts` | ✅ 正常 |
| 确认报警 | `ackAlert` | ✅ 正常 |
| 库存报警 | `adminInventoryManage` | ✅ 正常 |
| 获取工厂列表 | `getFactories` | ✅ 正常 |
| 离线队列同步 | `submitReplacementLog` | ✅ 正常 |

## 测试结论

- **核心流程**（扫码 → 更换登记 → 记录列表 → 报警列表 → 确认报警）= **可用** ✅
- **看板及下钻页面** = **不可用** 🔴（缺少 7 个云函数）
- **报警详情页** = **不可用** 🔴（缺少 getAlertDetail 云函数）

## 建议优先级

1. 🔴 **紧急修复**：补齐 `getDashboard`、`getAlertDetail` 这两个影响核心体验的云函数
2. 🔴 **重要修复**：补齐 `getPartUsageDetail`、`getAssetUsageDetail`、`listAssets`、`adminDashboard`
3. 🟡 **后续优化**：补齐 `getAssetAlerts`

> 如需我补齐这些缺失的云函数，请告知，我可以立即开始开发。

---

# 第二部分：PC 管理端测试


## 一、审查概览

| 审查项 | 结果 |
|--------|------|
| API 接口覆盖 | 52 个前端 API 全部有 pcGateway 后端实现 ✅ |
| 路由配置 | 16 条路由，角色权限守卫完整 ✅ |
| 登录认证 | JWT 签发/验证逻辑完整，角色限制 PC 登录 ✅ |
| Pinia 状态 | auth + app 两个 Store 功能完备 ✅ |

---

## 二、发现并修复的问题（共 8 项）

### 🔴 P0 — 关键功能不可用

#### 问题 1：数据看板缺少 3 个关键数据集
- **影响**：看板页 4 个模块完全空白
  - M4「工程师工作量」图表为空
  - M5「最近 7 天更换趋势」折线图为空  
  - M6「报警设备分布」区域为空
  - 顶部「活跃工程师」卡片永远显示 0
- **原因**：`pcGateway.getDashboardStats` 未返回 `engineerWorkload`、`dailyTrend`、`alertsByAsset` 三个字段
- **修复**：在 getDashboardStats 中补充：
  - `engineerWorkload`：从 replacement_logs 按报告人分组统计
  - `dailyTrend`：计算最近 7 天每日更换次数
  - `alertsByAsset`：从 OPEN 报警按设备分组统计

#### 问题 2：AI 分析报告页面完全不可用
- **影响**：点击「生成报告」按钮后永远显示空白或报错
- **原因**：`pcGateway.getAIReport` 仅查询缓存表 `ai_reports`，无实际报告生成逻辑。当缓存不存在时返回 `{ report: null }`
- **修复**：完整重写 getAIReport：
  - 新增 `aggregateMonthData` 聚合函数：统计当月/上月的更换次数、配件消耗、报警、成本、人员等全维度数据
  - 新增 `buildReport` 报告生成函数：生成 7 个维度的分析报告（设备健康总览、历史对比、配件分析、设备热点、成本分析、人员负荷、报警闭环）
  - 支持 `scope: 'factory'` 和 `scope: 'summary'` 两种报告范围
  - 返回 `{ summaryText, sections, history, stats, prevStats, factoryLabel, byFactory }`

#### 问题 3：出入库记录页面看不到数据
- **影响**：PC 端「出入库记录」页面入库和出库记录均为空
- **原因**：集合名不匹配
  - pcGateway 读写 `inventory_logs`（错误）
  - `adminInventoryInbound` 写入 `inventory_inbound_logs`（正确）
  - `submitReplacementLog` 写入 `inventory_outbound_logs`（正确）
- **修复**：
  - `listInboundLogs` → 从 `inventory_inbound_logs` 读取
  - `listOutboundLogs` → 从 `inventory_outbound_logs` 读取
  - `inventoryInbound` → 写入 `inventory_inbound_logs`
  - `getInventoryTrend` → 分别查询两个集合
  - `getInventorySummary` → 从正确集合计算入库/出库金额
  - `ALL_COLLECTIONS` 数组同步更新

---

### 🟡 P1 — 数据显示不完整

#### 问题 4：报警列表缺少「确认人」显示
- **影响**：报警列表中已确认的报警「确认人」列显示空白，导出 Excel 也缺少确认人信息
- **原因**：`pcGateway.listAlerts` 返回原始数据，未将 `ackByUserId` 解析为 `ackByName`
- **修复**：在 listAlerts 中查询 users 表，将 `ackByUserId` 映射为 `ackByName`，同时确保 `assetName`/`partName` 字段存在

#### 问题 5：部位复制成功提示显示 undefined
- **影响**：复制设备部位后，成功提示显示 "复制成功：undefined 个部位，undefined 条映射"
- **原因**：前端期望 `{ copiedLocations, copiedMaps }`，pcGateway 返回 `{ copiedCount }`
- **修复**：`copyLocations` 返回 `{ copiedLocations, copiedMaps }` 分别统计部位和映射数量

#### 问题 6：配件使用成本排名全部显示 ¥0
- **影响**：看板「设备配件使用金额 TOP 10」和库存页面的成本排名全部为 0
- **原因**：
  - `getMonthlyCostRanking` 从 `replacement_logs` 取 `item.unitPrice`，但实际出库成本存在 `inventory_outbound_logs` 的 `totalCost` 字段
  - `getPartUsageCostList` 缺少 `partCode`、`specModel`、`unit`、`percentage` 等前端需要的字段
- **修复**：
  - `getMonthlyCostRanking` 改为从 `inventory_outbound_logs` 按设备汇总成本
  - `getPartUsageCostList` 从 `inventory_outbound_logs` 按配件汇总，补充 parts 表数据和百分比计算

#### 问题 7：库存列表缺少配件详情和阈值显示异常
- **影响**：库存列表中配件型号、单位显示空白；低库存标记不生效
- **原因**：
  - `listInventory` 返回原始数据，缺少 `specModelSnapshot`、`unitSnapshot`、`avgUnitCost`、`totalCostValue`
  - 前端使用 `lowStockThreshold`，数据库存储 `threshold`，字段名不一致
- **修复**：
  - `listInventory` 查询 parts 表补充快照字段，统一映射 `threshold` → `lowStockThreshold`
  - `updateInventoryThreshold` 同时更新 `threshold` 和 `lowStockThreshold`
  - `listInventoryAlerts` 兼容两种字段名

#### 问题 8：部位配件映射缺少配件编号
- **影响**：部位管理页面配件编号列显示空白
- **原因**：`listLocationPartMap` 返回原始映射数据，`partCode` 未存储在映射表中
- **修复**：`listLocationPartMap` 查询 parts 表，补充 `partName` 和 `partCode`

---

## 三、修改文件清单

| 文件 | 修改类型 | 修改内容 |
|------|----------|----------|
| `cloudfunctions/pcGateway/index.js` | 修改 | 修复上述 8 个问题 |

### pcGateway 修改的函数列表

| 函数名 | 修改说明 |
|--------|----------|
| `ALL_COLLECTIONS` | 替换 `inventory_logs` → `inventory_inbound_logs` + `inventory_outbound_logs` |
| `getDashboardStats` | 新增 `engineerWorkload`、`dailyTrend`、`alertsByAsset` |
| `getAIReport` | 完全重写，新增 `aggregateMonthData` 和 `buildReport` |
| `listAlerts` | 新增 `ackByName` 用户解析和字段富化 |
| `copyLocations` | 返回 `copiedLocations` + `copiedMaps` |
| `getMonthlyCostRanking` | 改为从 `inventory_outbound_logs` 按设备汇总 |
| `getPartUsageCostList` | 完全重写，从出库记录按配件汇总 + 补充字段 |
| `listInventory` | 新增 parts 表查询补充快照字段和阈值映射 |
| `updateInventoryThreshold` | 同时更新 `threshold` 和 `lowStockThreshold` |
| `listInventoryAlerts` | 兼容 `lowStockThreshold` 和 `threshold` |
| `listLocationPartMap` | 新增 parts 表查询补充 `partCode` |
| `inventoryInbound` | 改写入 `inventory_inbound_logs`，支持批量入库 |
| `listInboundLogs` | 改查 `inventory_inbound_logs` |
| `listOutboundLogs` | 改查 `inventory_outbound_logs` |
| `getInventoryTrend` | 改查两个正确集合 |
| `getInventorySummary` | 新增入库/出库金额和库存总价值计算 |

---

## 四、部署步骤

修复内容全部在 `cloudfunctions/pcGateway/index.js` 一个文件中，部署步骤如下：

1. 打开微信开发者工具
2. 右键 `cloudfunctions/pcGateway` → **「上传并部署：云端安装依赖」**
3. 等待部署完成
4. 刷新 PC 管理端页面验证

> **注意**：本次仅需重新部署 `pcGateway` 这一个云函数

---

## 五、PC 端测试用例执行预估

修复后各模块测试预期：

| 模块 | 测试用例数 | 修复前预期 | 修复后预期 |
|------|-----------|-----------|-----------|
| 登录/认证 | 8 | ✅ 全通过 | ✅ 全通过 |
| 数据看板 - 基础统计 | 5 | ✅ 通过 | ✅ 通过 |
| 数据看板 - 图表 | 6 | ❌ 4个图表空白 | ✅ 全通过 |
| 数据看板 - 下钻 | 4 | ✅ 通过 | ✅ 通过 |
| 数据看板 - 库存概览 | 4 | ❌ 金额显示0 | ✅ 全通过 |
| AI 分析报告 | 6 | ❌ 完全不可用 | ✅ 全通过 |
| 更换记录 | 5 | ✅ 通过 | ✅ 通过 |
| 报警管理 | 6 | ⚠ 确认人空白 | ✅ 全通过 |
| 设备管理 | 8 | ✅ 通过 | ✅ 通过 |
| 部位管理 | 6 | ⚠ 复制提示异常 | ✅ 全通过 |
| 配件字典 | 6 | ✅ 通过 | ✅ 通过 |
| 阈值管理 | 4 | ✅ 通过 | ✅ 通过 |
| 用户管理 | 6 | ✅ 通过 | ✅ 通过 |
| 工厂管理 | 4 | ✅ 通过 | ✅ 通过 |
| 库存管理 | 8 | ❌ 阈值显示异常 | ✅ 全通过 |
| 入库登记 | 4 | ⚠ 记录不同步 | ✅ 全通过 |
| 出入库记录 | 4 | ❌ 列表为空 | ✅ 全通过 |
| AI 设置 | 3 | ✅ 通过 | ✅ 通过 |
| **合计** | **97** | **❌ 多处异常** | **✅ 预期全通过** |

---

# 第三部分：跨端一致性与安全测试


## 一、跨端数据流审查结果

### 7 条数据流逐一验证

| # | 数据流 | 集合一致 | 字段兼容 | 修复前状态 | 修复后状态 |
|---|--------|:--------:|:--------:|:----------:|:----------:|
| 1 | 小程序提交更换记录 → PC端查看 | ✅ | ✅ | ✅ 通过 | ✅ 通过 |
| 2 | 小程序触发报警 → PC端查看 | ✅ | ✅ | ✅ 通过 | ✅ 通过 |
| 3 | PC端确认报警 → 小程序同步 | ✅ | ✅ | ✅ 通过 | ✅ 通过 |
| 4 | PC端新增设备 → 小程序扫码 | ✅ | ✅ | ✅ 通过 | ✅ 通过 |
| 5 | PC端修改阈值 → 小程序报警触发 | ✅ | ✅ | ✅ 通过 | ✅ 通过 |
| 6 | PC端新增用户 → 小程序绑定 | ✅ | ✅ | ✅ 通过 | ✅ 通过 |
| 7 | PC端禁用用户 → 小程序阻断 | ✅ | ⚠ | ⚠ 提示不准确 | ✅ 通过 |

---

## 二、发现并修复的问题（共 5 项）

### 🔴 P0 — 看板下钻 Drawer 全部崩溃

#### 问题 1：「设备报警明细」Drawer 崩溃 (getDashboardAssetAlerts)
- **影响**：点击看板 M6 报警分布中的任何设备 → JavaScript Error `Cannot read properties of undefined (reading 'forEach')`
- **原因**：pcGateway 返回 `{ list: alerts }`，前端期望 `{ alerts: [...] }`
- **二级问题**：报警数据缺少 `partCode`、`unit`、`overQty`、`overRate`、`logs` 字段，Drawer 中多处显示 undefined
- **修复**：
  - 返回字段改为 `{ alerts, assetName, assetNo, workshop, yearMonth, openCount }`
  - 新增 parts 表查询补充 `partCode`、`unit`
  - 新增 replacement_logs 查询关联消耗明细
  - 计算 `overQty` 和 `overRate`

#### 问题 2：「配件消耗明细」Drawer 数据结构完全不对 (getDashboardPartDetail)
- **影响**：点击看板 M2 配件 TOP5 任何柱条 → Drawer 空白或崩溃
- **原因**：pcGateway 返回 `{ list: [...flat logs...] }`，前端期望 `{ partName, partCode, unit, totalQty, yearMonth, byAsset: [{assetId, assetName, qtySum, percentage, isOverThreshold, threshold, logs}] }`
- **修复**：完整重写，按设备分组统计，补充 parts/assets/thresholds 关联查询

#### 问题 3：「设备消耗明细」Drawer 数据结构完全不对 (getDashboardAssetDetail)
- **影响**：点击看板 M3 设备 TOP5 任何柱条 → Drawer 空白或崩溃
- **原因**：pcGateway 返回 `{ list: [...raw logs...] }`，前端期望 `{ assetName, assetNo, workshop, yearMonth, totalLogCount, totalPartTypes, byPart: [{partSkuId, partName, partCode, unit, qtySum, threshold, usageRate, isOverThreshold, logs}] }`
- **修复**：完整重写，按配件分组统计，补充 parts/assets/thresholds 关联查询

### 🟡 P1 — 禁用用户体验问题

#### 问题 4：禁用用户在小程序看到"未绑定"而非"已禁用" (getMe)
- **影响**：被 PC 端管理员禁用的用户打开小程序，看到"当前微信未绑定账号"的错误提示，用户困惑以为是绑定问题
- **原因**：`getMe` 云函数查询条件 `where({ openid, status: 'active' })` 直接过滤掉了禁用用户，导致代码中 `USER_DISABLED` 分支永远不会执行（死代码）
- **修复**：
  - 去掉查询中的 `status: 'active'` 过滤条件
  - 先查 openid → 如果用户存在但状态为 disabled → 返回 `USER_DISABLED`
  - 用更明确的提示文案："您的账号已被管理员禁用，如有疑问请联系管理员"

#### 问题 5：小程序未专门处理 USER_DISABLED 错误码 (app.js)
- **影响**：即使 getMe 返回 `USER_DISABLED`，小程序也只是显示一个短暂的 Toast，用户可能看不清
- **修复**：在 app.js 中新增 `USER_DISABLED` 分支处理，使用 `wx.showModal` 弹窗明确告知

---

## 三、修改文件清单

| 文件 | 修改类型 | 修改内容 |
|------|----------|----------|
| `cloudfunctions/pcGateway/index.js` | 修改 | 修复 3 个看板下钻函数的数据结构 |
| `cloudfunctions/getMe/index.js` | 修改 | 修复禁用用户检测逻辑 |
| `miniprogram/app.js` | 修改 | 新增 USER_DISABLED 错误处理 |

### pcGateway 修改的函数

| 函数名 | 修改说明 |
|--------|----------|
| `getDashboardAssetAlerts` | 完全重写：返回 `alerts` 字段 + 富化 + 关联更换记录 |
| `getDashboardPartDetail` | 完全重写：按设备分组统计 + 阈值对比 + 消耗明细 |
| `getDashboardAssetDetail` | 完全重写：按配件分组统计 + 阈值对比 + 消耗明细 |

---

## 四、安全与权限审查结果

| # | 测试项 | 审查结果 | 说明 |
|---|--------|:--------:|------|
| 1 | 工程师角色权限 | ✅ | 小程序按角色控制按钮显示 (auth.js canAck/canManage) |
| 2 | 查看员角色权限 | ✅ | Viewer 无提交权限 |
| 3 | 主管角色权限 | ✅ | 可 ACK 报警 + 查看 AI 报告 |
| 4 | 管理员角色权限 | ✅ | pcGateway adminOnlyActions 数组限制 |
| 5 | PC端非管理员调用管理员API | ✅ | pcGateway 统一权限检查，返回 PERMISSION_DENIED |
| 6 | 小程序 openid 校验 | ✅ | getMe/submitReplacementLog 等均校验 openid |
| 7 | JWT Token 过期 | ✅ | 前端 auth store 检查 exp，过期自动清除跳转登录 |
| 8 | 密码安全 | ✅ | SHA256 哈希存储 |
| 9 | 生产环境无测试数据暴露 | ✅ | IS_DEV=false, 登录页无测试账号提示 |
| 10 | PC端 ackAlert 缺少角色检查 | ⚠ 注意 | pcGateway ackAlert 未在 adminOnlyActions 中但允许所有已登录用户执行。小程序端 ackAlert 限制了 Supervisor/Management/Admin 角色。建议统一。 |

---

## 五、部署步骤

需要部署 **3 个** 云函数：

1. 右键 `cloudfunctions/pcGateway` → **「上传并部署：云端安装依赖」**
2. 右键 `cloudfunctions/getMe` → **「上传并部署：云端安装依赖」**
3. 重新上传小程序代码（`miniprogram/app.js` 有修改）

---

## 六、跨端测试用例预期

| 测试用例 | 修复前 | 修复后 |
|----------|:------:|:------:|
| 小程序提交更换记录 → PC 端查看 | ✅ | ✅ |
| 小程序触发报警 → PC 端查看 | ✅ | ✅ |
| PC 端确认报警 → 小程序查看 | ✅ | ✅ |
| PC 端新增设备 → 小程序扫码 | ✅ | ✅ |
| PC 端修改阈值 → 小程序触发报警 | ✅ | ✅ |
| PC 端新增用户 → 小程序绑定 | ✅ | ✅ |
| PC 端禁用用户 → 小程序操作 | ⚠ 提示不准确 | ✅ 明确提示"已禁用" |
| PC 看板配件下钻 → Drawer 显示 | ❌ 崩溃 | ✅ 正常显示 |
| PC 看板设备下钻 → Drawer 显示 | ❌ 崩溃 | ✅ 正常显示 |
| PC 看板报警下钻 → Drawer 显示 | ❌ 崩溃 | ✅ 正常显示 |
