# 康洁工程部小程序 — 正式上线步骤（最新版）

按顺序做下面几步即可。不用懂编程。

---

**「云开发」在哪里？**  
微信开发者工具窗口**最上面一栏**（和「编译」「预览」「上传」同一排）有个 **「云开发」** 按钮，点一下打开云开发面板。下面说的「点云开发」都是点这里。

---

## 第一步：部署云函数（把云函数传到云端）

- **说明**：环境 ID 已填好（cloud1-0g0grbwt8c230b0d），不用再改。

**优先试一键上传：**

1. 用**微信开发者工具**打开本项目。
2. 在**左侧**找到 **「cloudfunctions」** 这个文件夹（不要点进去）。
3. 在 **cloudfunctions** 上 **右键**，看菜单里有没有 **「上传并部署：所有云函数」** 或 **「同步云端」**。
4. 有的话点它，等提示成功即可；然后跳到下面第 5 步检查。

**如果没有「所有」或「同步」选项**，就逐个上传：

- 点开 **cloudfunctions**，对里面每一个小文件夹（getMe、getAssetByQr、submitReplacementLog 等）**右键** → **「上传并部署：云端安装依赖」**，直到全部传完。

5. 传完后：点最上面一栏 **「云开发」** → 在面板里点 **「云函数」**，看列表里是否都有这些名字，有就表示成功。

---

## 第二步：建数据库集合

1. 点最上面一栏 **「云开发」**，在面板里点 **「数据库」**。
2. 如果是空的，点 **「添加集合」**，按下面名字**一个一个**新建（每个名字建一个集合）：
   - **assets**
   - **parts**
   - **replacement_logs**
   - **alerts**
   - **users**
3. 如果后面小程序或云函数报错说缺某个集合，再按提示补建即可。建好后集合是空的也可以。

---

## 第三步：删测试数据（可选）

只有**之前用过「初始化测试数据」或自己加过测试数据**才需要做。

1. 点 **「云开发」** → **「数据库」**。
2. 逐个点进每个集合，如果里面是测试数据：
   - **勾选全部** → 点 **「删除」**。
3. 若只想删业务数据、保留设备和用户：只清空 **replacement_logs**、**monthly_part_usage**、**alerts**；**assets**、**parts**、**users** 可保留。

---

## 第四步：导入真实数据（设备、配件、用户）

- 有现成 Excel 表：按小程序「数据管理」页的**导入模板**整理好，在手机小程序里用「数据管理 → 导入」上传。
- 没有模板：让技术人员在云开发控制台或通过云函数帮你导入设备、配件、用户等基础数据。

---

## 第五步：手机预览测试

1. 在微信开发者工具右上角点 **「预览」**，用手机微信扫码。
2. 在手机上试：能打开、能扫码进设备、能提交更换记录、在「记录」和「看板」能看到数据。
3. 有问题记下来，交给技术人员排查。

---

## 第六步：上传代码并提交审核、发布

1. 微信开发者工具右上角点 **「上传」**。
2. 版本号填 **1.0.0**，备注填 **康洁工程部正式版**，确定上传。
3. 浏览器打开 **微信公众平台**（mp.weixin.qq.com），登录 → 进入你的小程序 → **版本管理**。
4. 在「开发版本」里找到刚传的版本，点 **「提交审核」**，按页面提示选类目、填说明后提交。
5. 审核通过后，在「版本管理」里点 **「发布」**，即正式上线。

---

## 打勾清单

- [x] 环境 ID 已配置（cloud1-0g0grbwt8c230b0d）
- [ ] 第一步：所有云函数已「上传并部署」成功
- [ ] 第二步：数据库已建好需要的集合
- [ ] 第三步：测试数据已删除（如有）
- [ ] 第四步：已导入或录入真实设备、配件、用户
- [ ] 第五步：手机预览测试通过
- [ ] 第六步：已上传 → 提交审核 → 发布

做完这六步即完成上线。某一步不清楚时，告诉技术人员是第几步、卡在哪里即可。
