# 康洁工程部 — 完整上线指南（小程序 + PC 管理端）

本文档包含**从零到完成**的全部步骤。按顺序做即可。

> **当前进度**：小程序已提交审核，PC 端已部署到静态网站托管并可通过 HTTP 访问服务正常登录。

---

## 整体架构

```
┌─────────────────────┐      ┌──────────────────────────────┐
│  手机微信小程序       │      │  PC 管理端（浏览器）            │
│  扫码、登记、看板     │      │  看板、报警、设备管理等          │
└──────────┬──────────┘      └───────────┬──────────────────┘
           │ wx.cloud.callFunction              │ HTTP 访问服务
           ▼                                    ▼
┌──────────────────────────────────────────────────────────┐
│              云函数（同一套）                                │
│   getMe · listAlerts · adminPcLogin · pcGateway · ...    │
└──────────┬───────────────────────────────────────────────┘
           ▼
┌──────────────────────────────────────────────────────────┐
│              云开发数据库（同一套数据）                       │
│   assets · parts · users · replacement_logs · alerts      │
│          环境 ID: cloud1-0g0grbwt8c230b0d                 │
└──────────────────────────────────────────────────────────┘
```

小程序和 PC 端**共用同一个数据库**，数据互通。

---

# 第一部分：小程序上线（已基本完成）

> 如果小程序已提交审核，跳到第二部分。

## 步骤 1：部署云函数

1. 用**微信开发者工具**打开 `kangjie-engineering` 项目。
2. 左侧找到 `cloudfunctions` 文件夹。
3. 在 `cloudfunctions` 上**右键** → 选 **「上传并部署：所有云函数」**。
4. 部署后在顶部点 **「云开发」** → **「云函数」**，确认列表中有所有函数。

## 步骤 2：建数据库集合

1. 点 **「云开发」** → **「数据库」**。
2. 点 **「添加集合」**，逐个新建以下集合（如已存在则跳过）：
   - `assets`、`parts`、`users`、`replacement_logs`、`alerts`
   - `factories`、`asset_part_thresholds`、`asset_locations`、`location_part_map`

## 步骤 3：导入真实数据

至少需要：
- `users` 集合里有 1 条管理员记录（含 openid 绑定）
- `assets` 集合里有设备
- `parts` 集合里有配件

## 步骤 4：手机预览测试

1. 微信开发者工具右上角点 **「预览」**，用手机微信扫码。
2. 测试：扫码进设备 → 提交更换记录 → 在「记录」「看板」查看。

## 步骤 5：提交审核、发布

1. 微信开发者工具右上角点 **「上传」**，版本号填 `1.0.0`。
2. 浏览器打开 [微信公众平台](https://mp.weixin.qq.com) → 你的小程序 → **版本管理**。
3. 在「开发版本」里点 **「提交审核」**。
4. 审核通过后点 **「发布」**。

---

# 第二部分：PC 管理端上线

> PC 端和小程序审核**互不影响**，可同时进行。
> 部署完成后，PC 端会一直在云上运行，**不需要你的电脑一直开着**。

---

## PC 步骤 1：部署两个新云函数

在**微信开发者工具**中：

1. 左侧展开 `cloudfunctions` 文件夹。
2. 找到 **adminPcLogin** 文件夹 → **右键** → **「上传并部署：云端安装依赖」**。
3. 找到 **pcGateway** 文件夹 → **右键** → **「上传并部署：云端安装依赖」**。
4. 部署后，点顶部 **「云开发」** → **「云函数」**，确认列表中新增了这两个。

---

## PC 步骤 2：给管理员设置 PC 登录密码

在云开发控制台的数据库中：

1. 点 **「云开发」** → **「数据库」** → 点进 **users** 集合。
2. 找到你要用来登录 PC 端的用户记录（Admin 或 Supervisor）。
3. 点击该记录，**新增一个字段**：

   | 字段名 | 类型 | 值（示例） |
   |--------|------|-----------|
   | pcPassword | string | admin123 |

4. 同时确认该记录有这些字段：

   | 字段名 | 要求 |
   |--------|------|
   | username | 登录用户名，如 `admin` |
   | role | `Admin` 或 `Supervisor` 或 `Management` |
   | status | `active` |

**完整示例**（一条 users 记录应包含）：
```json
{
  "userId": "u001",
  "username": "admin",
  "displayName": "张管理",
  "role": "Admin",
  "status": "active",
  "pcPassword": "admin123"
}
```

---

## PC 步骤 3：开通静态网站托管 + 安全域名

### 3.1 开通静态网站托管

1. 在微信开发者工具 → 云开发控制台中，找到 **「静态网站」**。
2. 点 **「前往开通」** 或 **「开通静态网站」**。
3. 开通后会看到一个**默认域名**，类似：`cloud1-0g0grbwt8c230b0d-xxxx.tcloudbaseapp.com`。
4. **记下这个域名**，后面要用。

### 3.2 配置安全域名

1. 在腾讯云 CloudBase 控制台 → **「安全来源」**。
2. 把静态网站域名加到安全域名列表中。

---

## PC 步骤 3.5：开通 HTTP 访问服务（已购买个人版）

> 购买云开发**个人版**后，用 HTTP 访问服务调用云函数，可避免匿名登录的 PERMISSION_DENIED。

在 **腾讯云 CloudBase 控制台**（[tcb.cloud.tencent.com](https://tcb.cloud.tencent.com)）操作：

1. 进入你的环境 → 左侧 **「环境管理」** → **「环境配置」** → **「HTTP 访问服务」**。
2. 将 **「HTTP 访问服务」** 开关**打开**。
3. 点击 **「新建」**，添加两条「域名关联资源」：

   | 触发路径 | 关联资源类型 | 关联云函数 | 身份认证 |
   |----------|-------------|-----------|----------|
   | `/adminPcLogin` | 云函数 | `adminPcLogin` | **关闭**（允许未登录访问） |
   | `/pcGateway` | 云函数 | `pcGateway` | **关闭**（允许未登录访问） |

4. **域名**选择 HTTP 访问服务的默认域名（如 `cloud1-0g0grbwt8c230b0d-1404561581.ap-shanghai.app.tcloudbase.com`）。
5. 保存后，确认列表里这两条路径状态为「正常」。

> 注意：默认域名有有效期，记得续期。正式使用建议绑定自定义域名。

---

## PC 步骤 4：构建并上传 PC 管理端

### 4.1 构建生产版本

```bash
cd /Users/johnsfile/Desktop/工程部小程序/kangjie-engineering/pc-admin
npm install    # 首次需要
npm run build
```

看到 `built in x.xxs` 即成功。构建产物在 `dist/` 文件夹中。

### 4.2 上传到静态网站托管

1. 回到云开发控制台 → **「静态网站」**。
2. 点 **「上传文件」** 或 **「管理文件」**。
3. 把 `pc-admin/dist/` 文件夹里的**所有文件和文件夹**上传到**根目录**：
   - `index.html`
   - `assets/` 文件夹（里面有 js、css 等）
   - `vite.svg`

> 注意：是上传 dist 里面的内容，不是上传 dist 文件夹本身。上传时勾选「覆盖同路径文件」。

---

## PC 步骤 5：访问并登录

1. 浏览器打开你的静态网站域名：
   ```
   https://cloud1-0g0grbwt8c230b0d-1404561581.tcloudbaseapp.com
   ```

2. 输入第二步设置的用户名和密码登录：
   - 用户名：`admin`
   - 密码：`admin123`

3. 登录后应该能看到：
   - **数据看板**：显示更换记录统计
   - **更换记录**：小程序提交的记录在这里都能看到
   - **报警管理**：可以查看和确认报警
   - **设备管理**：查看设备列表
   - **配件字典**：查看配件列表
   - **用户管理**（管理员才有）：查看用户列表

> 这个网址 **7x24 小时可用**，不需要你的电脑开着。

---

# 第三部分：日常使用

## 日常使用（已部署到云端）

直接打开浏览器，访问静态网站域名，输入用户名密码登录即可。

- 不需要开终端
- 不需要启动任何服务
- 任何电脑、手机浏览器都能访问

## 本地开发模式（改代码/调试时用）

**方式一：连真实云数据（需要 pc-server）**

终端 1（PC 网关）：
```bash
cd /Users/johnsfile/Desktop/工程部小程序/kangjie-engineering/pc-server
npm start
```

终端 2（PC 管理端）：
```bash
cd /Users/johnsfile/Desktop/工程部小程序/kangjie-engineering/pc-admin
npm run dev
```

浏览器打开 http://localhost:3000 。

**方式二：Mock 模式（不连云开发）**

```bash
cd /Users/johnsfile/Desktop/工程部小程序/kangjie-engineering/pc-admin
npm run dev -- --mode mock
```

## 更新 PC 管理端

如果以后修改了 PC 端代码，需要重新部署：

```bash
cd /Users/johnsfile/Desktop/工程部小程序/kangjie-engineering/pc-admin
npm run build
```

然后去云开发控制台 → 静态网站 → 重新上传 `dist/` 里的文件。

---

# 打勾清单

## 小程序
- [x] 环境 ID 已配置（cloud1-0g0grbwt8c230b0d）
- [x] 云函数已部署（14 个）
- [x] 数据库已建好集合
- [ ] 真实数据已导入
- [x] 已提交审核
- [ ] 审核通过后点「发布」

## PC 管理端
- [x] PC 步骤 1：部署 adminPcLogin 和 pcGateway 云函数
- [x] PC 步骤 2：在 users 集合给管理员加 pcPassword 字段
- [x] PC 步骤 3：开通静态网站托管 + 配置安全域名
- [x] PC 步骤 3.5：开通 HTTP 访问服务，绑定 /adminPcLogin、/pcGateway
- [x] PC 步骤 4：`npm run build` 并上传 dist/ 到静态网站托管
- [x] PC 步骤 5：打开网址，用用户名密码登录成功
- [ ] 验证：登录后能看到真实数据（需要先在数据库添加设备/配件等）

---

# 常见问题

**Q：打开网址显示空白或报错**
A：检查静态网站里的文件是否上传完整（index.html 和 assets/ 文件夹）。如果 CSS 没加载（图标很大），说明 assets/ 没上传成功。

**Q：登录时提示「用户名或密码错误」**
A：去云开发数据库 → users 集合，检查：
- `username` 字段是否和输入的一样
- `pcPassword` 字段是否和输入的密码一样
- `role` 是否为 Admin / Supervisor / Management
- `status` 是否为 active

**Q：登录时提示 PERMISSION_DENIED**
A：请完成 PC 步骤 3.5（开通 HTTP 访问服务并绑定两个路径），且身份认证选「关闭」。然后重新部署 adminPcLogin、pcGateway 云函数，再重新构建并上传 PC 前端。

**Q：登录成功但页面没数据**
A：数据库里可能还没有真实业务数据。先在小程序里提交几条更换记录，PC 端就能看到了。

**Q：部署新云函数会影响正在审核的小程序吗？**
A：**不会**。adminPcLogin 和 pcGateway 是新增的云函数，小程序的代码不调用它们，互不影响。

**Q：PC 端需要 AppSecret 吗？**
A：**不需要**。已购买个人版使用 HTTP 访问服务直连云函数，不需要 AppSecret。

**Q：流量会超标吗？**
A：个人版额度充足。PC 管理端总共才几 MB，内部几个人用完全够。

**Q：以后改了代码怎么更新？**
A：重新 `npm run build`，然后去云开发控制台 → 静态网站 → 重新上传 dist/ 里的文件即可。

**Q：左侧导航栏是全黑的/没有菜单项**
A：需要重新构建并上传。路由配置中需要包含 `meta.title` 和 `meta.icon` 才能在导航栏显示。

---

某一步不清楚时，记下**「第几部分、第几步、卡在哪里」**告诉技术人员即可。
