/* pages/dashboard/part-detail.wxss */
.detail-header {
  text-align: center;
  padding: 32rpx 24rpx;
}

.detail-title {
  font-size: 36rpx;
  font-weight: 700;
  margin-bottom: 8rpx;
}

.detail-meta {
  display: flex;
  justify-content: center;
  gap: 24rpx;
  margin-bottom: 24rpx;
}

.detail-code {
  font-size: 24rpx;
  color: var(--weui-FG-2);
}

.detail-month {
  font-size: 24rpx;
  color: var(--weui-FG-2);
}

.detail-total {
  display: flex;
  align-items: baseline;
  justify-content: center;
  gap: 8rpx;
}

.total-num {
  font-size: 56rpx;
  font-weight: 700;
  color: var(--weui-BRAND);
  line-height: 1;
}

.total-unit {
  font-size: 28rpx;
  color: var(--weui-FG-1);
}

.total-label {
  font-size: 24rpx;
  color: var(--weui-FG-2);
  margin-left: 8rpx;
}

.list-title {
  font-size: 28rpx;
  font-weight: 500;
  margin-bottom: 20rpx;
  color: var(--weui-FG-1);
}

.detail-list {
  /* list container */
}

.detail-item {
  display: flex;
  align-items: center;
  padding: 16rpx 0;
  border-bottom: 1rpx solid var(--weui-BG-1);
}

.detail-item:last-child {
  border-bottom: none;
}

.detail-item-left {
  display: flex;
  align-items: center;
  min-width: 280rpx;
  flex-shrink: 0;
}

.item-rank {
  width: 48rpx;
  font-size: 28rpx;
  font-weight: 600;
  color: var(--weui-BRAND);
  flex-shrink: 0;
}

.item-info {
  flex: 1;
  min-width: 0;
}

.item-name {
  display: block;
  font-size: 28rpx;
  font-weight: 500;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.item-sub {
  display: block;
  font-size: 22rpx;
  color: var(--weui-FG-2);
  margin-top: 2rpx;
}

.item-bar-wrap {
  flex: 1;
  height: 16rpx;
  background: var(--weui-BG-1);
  border-radius: 8rpx;
  margin: 0 16rpx;
  overflow: hidden;
}

.item-bar {
  height: 100%;
  background: linear-gradient(90deg, #07C160, #06AD56);
  border-radius: 8rpx;
  min-width: 4rpx;
}

.item-qty {
  font-size: 26rpx;
  color: var(--weui-FG-0);
  font-weight: 500;
  min-width: 100rpx;
  text-align: right;
  flex-shrink: 0;
}
