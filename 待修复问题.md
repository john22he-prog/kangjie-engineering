# 待修复问题清单

> 记录于：2026-02-13
> 最后更新：2026-02-13
> 状态：全部已修复

---

## 问题 1：登录页面暴露测试账号密码 ✅ 已修复

**严重程度：** 高  
**位置：** 登录页面  
**修复方案：**
- 登录页通过 `import.meta.env.DEV` 判断环境，仅开发模式显示测试账号
- 生产构建时自动隐藏，不会暴露测试凭据
- **修改文件：** `pc-admin/src/views/login/index.vue`

---

## 问题 2：PC 端 AI 报告功能缺失 + 需支持多工厂汇总 ✅ 已修复

**严重程度：** 中  
**位置：** PC 端管理后台  
**修复方案：**
- AI 报告路由从 `hidden: true` 改为显示在侧边栏菜单中（带 TrendCharts 图标）
- 数据看板页面顶部新增「AI 分析报告」快捷入口按钮
- AI 报告页面已完整支持：管理员可选择"单工厂"或"全部汇总"模式，主管自动显示当前工厂
- **修改文件：** `pc-admin/src/router/index.js`、`pc-admin/src/views/dashboard/index.vue`

---

## 问题 3：用户管理 - 新增客户时缺少权限信息说明 ✅ 已修复

**严重程度：** 低  
**位置：** 用户管理 → 新增/编辑用户  
**修复方案：**
- 在角色选择下方添加 `el-alert` 组件，根据所选角色动态显示权限说明
- 包含管理员、主管、工程师、查看员四种角色的详细权限描述
- **修改文件：** `pc-admin/src/views/users/index.vue`

---

## 需求 4：PC 端设备管理增加 Excel 批量上传功能 ✅ 已实现（含关联关系）

**类型：** 新功能  
**位置：** PC 端管理后台 → 设备管理  
**实现方案：**
- 动态模板下载：点击「下载模板」时从数据库实时拉取配件字典和已有设备数据，生成 4 个 Sheet：
  - **Sheet 1「设备信息」**：设备ID、名称、编号、类型、车间、工厂（用于批量新增设备）
  - **Sheet 2「部位-配件关联」**：设备ID、设备名称、部位名称、配件编号、配件名称（用于配置设备多对多关联）
  - **Sheet 3「配件字典(参考)」**：从数据库实时拉取的全部配件列表，用户参考填写 Sheet 2
  - **Sheet 4「已有设备(参考)」**：从数据库实时拉取的已有设备列表，用户参考填写关联
- 模板文件名包含日期戳，每次下载均为最新数据
- 导入时解析两个 Sheet：设备信息 + 部位-配件关联
- 关联导入逻辑：按设备ID+部位名称分组 → 自动创建部位 → 创建部位-配件映射
- 配件编号校验：导入关联时校验配件编号是否在配件字典中存在，不存在则报错
- Tab 式预览：分别预览设备数据和关联数据
- **修改文件：** `pc-admin/src/views/assets/index.vue`

---

## 需求 5：用户管理页面增加权限控制 — 仅管理员可操作 ✅ 已修复

**类型：** 权限控制  
**位置：** PC 端管理后台 → 用户管理  
**修复方案：**
- **前端路由层面：** 已有 `meta: { roles: ['Admin'] }` 配置，非管理员自动重定向
- **侧边栏菜单：** MainLayout 已过滤角色权限，非管理员不显示「用户管理」和「工厂管理」入口
- **后端校验新增：** pcGateway 云函数增加管理员专属 action 权限检查（`listUsers`、`createUser`、`updateUser`、`disableUser`、`unbindOpenid`、`createFactory`、`updateFactory`），非 Admin 角色调用返回 PERMISSION_DENIED
- **新增后端函数：** pcGateway 中新增 `createUser`、`updateUser`、`disableUser`、`unbindOpenid` 处理函数
- **修改文件：** `cloudfunctions/pcGateway/index.js`

---

## 问题 6：PC 端缺少切换工厂的按钮 + 需支持多工厂汇总查看 ✅ 已修复

**类型：** 功能缺失  
**位置：** PC 端管理后台 → 顶部导航栏  
**修复方案：**
- 工厂选择器已存在于顶部导航栏（仅 Admin/Management 可切换）
- 新增「全部工厂（汇总）」选项，选择后所有页面数据显示汇总
- 切换后自动刷新页面，重新加载对应范围数据
- 用户选择会保存到 localStorage，下次登录自动恢复
- **修改文件：** `pc-admin/src/layouts/MainLayout.vue`

---

## 问题 7：小程序扫码页面图标位置偏上 ✅ 已修复

**类型：** UI 优化  
**位置：** 小程序 → 扫码录入页面  
**修复方案：**
- 将 `.scan-hero` 容器从固定 padding 改为 flex 垂直居中布局
- 设置 `min-height: 50vh` + `justify-content: center`，使扫码按钮自动居中
- **修改文件：** `miniprogram/pages/scan/index.wxss`

---

## 问题 8：PC 端管理后台手机浏览器访问页面扭曲 ✅ 已修复

**类型：** 兼容性问题  
**位置：** PC 端管理后台  
**修复方案：**
- App.vue 增加手机端检测（UA + 屏幕宽度判断）
- 手机端访问时显示友好提示页面：提示使用电脑浏览器访问
- 用户可点击「我知道了，继续访问」按钮跳过提示
- **修改文件：** `pc-admin/src/App.vue`

---

## 安全问题 9：JWT 密钥硬编码 ✅ 已修复

**严重程度：** 高  
**位置：** cloudfunctions/adminPcLogin、cloudfunctions/pcGateway  
**修复方案：**
- 优先从环境变量 `JWT_SECRET` 读取密钥
- 未设置时输出警告日志，并使用基于环境 ID 的动态默认密钥（而非固定字符串）
- 建议在云函数环境变量中配置一个强随机密钥
- **修改文件：** `cloudfunctions/adminPcLogin/index.js`、`cloudfunctions/pcGateway/index.js`

---

## 安全问题 10：密码明文存储和比较 ✅ 已修复

**严重程度：** 高  
**位置：** cloudfunctions/adminPcLogin  
**修复方案：**
- 登录时使用 SHA256 哈希比较密码
- 同时兼容旧的明文密码（过渡期），登录成功后自动将明文密码升级为哈希存储
- 新建用户时使用 SHA256 哈希存储密码（通过 pcGateway 的 createUser）
- **修改文件：** `cloudfunctions/adminPcLogin/index.js`、`cloudfunctions/pcGateway/index.js`

---

## 安全问题 11：CORS 配置过于宽松 ✅ 已修复

**严重程度：** 中  
**位置：** pc-server/index.js  
**修复方案：**
- CORS 从 `*` 改为白名单模式，仅允许 `CORS_ORIGINS` 环境变量中配置的域名
- 默认允许 `localhost:3000` 和 `localhost:5173`（开发环境）
- 部署后在 .env 中配置正式前端域名
- **修改文件：** `pc-server/index.js`、`pc-server/.env.example`

---

## 安全问题 12：部分云函数缺少权限校验 ✅ 已修复

**严重程度：** 中  
**位置：** 各独立云函数  
**修复方案：**
- `listReplacementLogs`、`listAlerts`、`getAssetByQr` 新增 openid 校验，无身份信息时拒绝访问
- `submitReplacementLog`、`ackAlert` 增加显式 openid 空值检查
- `getMe` 已有完整校验（无需修改）
- **修改文件：** `cloudfunctions/listReplacementLogs/index.js`、`cloudfunctions/listAlerts/index.js`、`cloudfunctions/getAssetByQr/index.js`、`cloudfunctions/submitReplacementLog/index.js`、`cloudfunctions/ackAlert/index.js`

---

## 问题 13：Token 无过期时间管理 ✅ 已修复

**严重程度：** 中  
**位置：** PC 端登录/认证逻辑  
**修复方案：**
- JWT Token 已有 7 天过期时间（后端生成时设置 exp 字段）
- 前端 auth store 启动时解析 JWT payload，检查 exp 字段是否过期
- Token 过期时自动清除 localStorage 并跳转登录页
- **修改文件：** `pc-admin/src/stores/auth.js`

---

## 需求 14：设备-配件多对多关联 Excel 批量导入，可选项实时同步 ✅ 已实现

**类型：** 新功能增强  
**位置：** PC 端管理后台 → 设备管理 → Excel 批量导入  
**描述：**
Excel 模板的可选项（配件编号、设备型号等）应跟着数据库中的设备和配件数据同步更新，确保批量导入时精准分配部位-配件关联关系。

**实现方案：**
- 下载模板时调用 `api.listParts()` 和 `api.listAssets()` 实时拉取数据
- Sheet 3（配件字典参考）包含所有可用配件的编号、名称、规格、单位，用户参照填写关联
- Sheet 4（已有设备参考）包含已有设备的 ID 和名称，可用于关联已有设备的部位配件
- 导入时配件编号与数据库进行交叉校验，不匹配时按行报错
- 每次下载模板都是最新数据快照，配件/设备增删改后重新下载即自动同步
- **修改文件：** `pc-admin/src/views/assets/index.vue`（与需求 4 同一文件）

---

## 修改文件汇总

### PC 管理端前端 (pc-admin/src/)
| 文件 | 修改内容 |
|------|---------|
| `views/login/index.vue` | 测试账号仅开发环境显示 |
| `views/dashboard/index.vue` | 添加 AI 报告入口按钮 |
| `views/users/index.vue` | 角色权限说明面板 |
| `views/assets/index.vue` | Excel 批量导入功能 |
| `layouts/MainLayout.vue` | 工厂选择器增加"全部工厂"选项 |
| `router/index.js` | AI 报告路由显示在菜单 |
| `stores/auth.js` | Token 过期自动检测 |
| `App.vue` | 手机端访问提示 |

### 云函数 (cloudfunctions/)
| 文件 | 修改内容 |
|------|---------|
| `adminPcLogin/index.js` | JWT 密钥改进 + 密码哈希比较 + 自动升级 |
| `pcGateway/index.js` | JWT 密钥改进 + 管理员权限校验 + 用户管理函数 |
| `listReplacementLogs/index.js` | 新增 openid 校验 |
| `listAlerts/index.js` | 新增 openid 校验 |
| `getAssetByQr/index.js` | 新增 openid 校验 |
| `submitReplacementLog/index.js` | 新增显式 openid 检查 |
| `ackAlert/index.js` | 新增显式 openid 检查 |

### 小程序 (miniprogram/)
| 文件 | 修改内容 |
|------|---------|
| `pages/scan/index.wxss` | 扫码按钮垂直居中 |

### 服务端 (pc-server/)
| 文件 | 修改内容 |
|------|---------|
| `index.js` | CORS 白名单限制 |
| `.env.example` | 新增 CORS_ORIGINS 配置说明 |

---

## 部署注意事项

1. **重新部署云函数：** 以下云函数需要重新上传部署
   - `adminPcLogin`（密码哈希 + JWT 密钥改进）
   - `pcGateway`（权限校验 + 用户管理 + JWT 密钥改进）
   - `listReplacementLogs`（openid 校验）
   - `listAlerts`（openid 校验）
   - `getAssetByQr`（openid 校验）
   - `submitReplacementLog`（openid 检查）
   - `ackAlert`（openid 检查）

2. **配置 JWT_SECRET 环境变量：** 在微信开发者工具 → 云开发控制台 → 云函数 → 环境变量中，为 `adminPcLogin` 和 `pcGateway` 添加 `JWT_SECRET` 环境变量（建议使用 32 位以上的随机字符串）

3. **重新构建 PC 管理端：** 运行 `npm run build` 后重新上传到静态网站托管

4. **PC-server 部署（如使用）：** 配置 .env 中的 `CORS_ORIGINS` 为正式域名
